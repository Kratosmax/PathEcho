<Window x:Class="PathEcho.Dialogs.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PathEcho" Width="540" SizeToContent="Height" MinHeight="230" MaxHeight="520"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="24"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold"/>
        <ScrollViewer Grid.Row="1" MaxHeight="280" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" LineHeight="21" Foreground="{StaticResource MutedBrush}"/>
        </ScrollViewer>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TertiaryButton" Click="OnTertiary" Visibility="Collapsed" Margin="0,0,8,0"/>
            <Button x:Name="SecondaryButton" Click="OnSecondary" Visibility="Collapsed" Margin="0,0,8,0"/>
            <Button x:Name="PrimaryButton" Click="OnPrimary" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
