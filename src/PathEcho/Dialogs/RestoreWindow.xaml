<Window x:Class="PathEcho.Dialogs.RestoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="回滚游戏存档" Width="620" Height="520" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="回滚游戏存档" FontSize="22" FontWeight="SemiBold"/>
        <TextBlock x:Name="VersionText" Grid.Row="1" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,18"/>
        <StackPanel Grid.Row="2">
            <TextBlock Text="恢复方式" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
            <ComboBox x:Name="ModeBox" SelectionChanged="OnModeChanged"/>
        </StackPanel>
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="仅恢复匹配文件（每行一个正则）" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
                <TextBox x:Name="IncludeBox" Height="82" AcceptsReturn="True" VerticalContentAlignment="Top"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="排除文件（每行一个正则）" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
                <TextBox x:Name="ExcludeBox" Height="82" AcceptsReturn="True" VerticalContentAlignment="Top"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="4" Background="#FFF6E8" BorderBrush="#E8C98F" BorderThickness="1" CornerRadius="5" Padding="12" Margin="0,16,0,0">
            <TextBlock x:Name="WarningText" Text="整目录恢复会先暂存完整版本，再用目录交换替换当前存档。" TextWrapping="Wrap" Foreground="#6F5017"/>
        </Border>
        <TextBlock Grid.Row="5" x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,12,0,0"/>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" Margin="0,0,8,0"/>
            <Button Content="检查并恢复" Style="{StaticResource PrimaryButton}" Click="OnRestore"/>
        </StackPanel>
    </Grid>
</Window>
