# PathEcho 0.5.0

PathEcho 0.5.0 增加可配置的自动备份通知、分层快照保留和版本文件清单，重点改善后台备份反馈和长期历史的可用性。

- 自动备份成功或失败时显示无焦点置顶小窗；手动备份不会重复弹出通知。
- 全局通知支持深色/浅色、目标显示器、四角位置和每个位置独立的 X/Y 微调，并提供即时预览和位置重置。
- 每个游戏可继承全局通知、关闭通知或使用独立设置；相同通知会合并计数，有界队列避免无限堆积。
- 快照清理由单一版本上限升级为“最近版本 + 每小时锚点 + 每日锚点”三层并集。
- 旧配置中的保留版本数直接兼容为最近版本；每小时和每日锚点可分别设为 0，现有快照格式无需迁移。
- 双击版本历史可查看文件清单，支持搜索并显示文件名、所在目录、文件修改时间、友好大小和精确字节数。
- 三层保留后仍按清单引用清理内容对象，相同内容继续只存一份。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。

重要：`v0.3.0` 及更早版本若出现旧更新器无法移动安装目录的问题，请手动覆盖安装一次。用户配置与备份不会被覆盖安装删除。
