# PathEcho 0.3.0

PathEcho 0.3.0 增加可筛选、搜索和编辑的游戏存档管理，并增强备份失败时的本地暂存与持续重试；同时修复自动更新交接可能中断的问题。

- 游戏备份首次读写失败后，会在默认备份目录的 `temp` 中建立完整校验副本，并以 5 秒间隔持续重试；每 10 次询问是否继续，退出可取消，停止写入时保留完整副本并报告位置。
- 旧版本清理失败只重试清理，不会重复创建新快照；临时目录和迁移目录清理不会递归进入重解析点。
- 版本历史支持按游戏 ID 筛选和搜索；大量历史记录合并刷新，避免逐条重建筛选造成卡顿，同名游戏也不会串在一起。
- 游戏存档支持选中后编辑和双击编辑；改变单独备份目录时复用校验迁移与失败回滚，其他字段修改只重启该游戏监听。
- 游戏页和历史页压缩最小列宽，920×620 最小窗口下仍能直接看到备份、打开和回档操作。

- 用落盘握手替代固定等待 750ms；外部更新器完成参数、目标和独占锁初始化后，主程序才退出。
- 更新器全程持有独占锁；普通启动会探测更新事务，阻止更新空窗期的重复启动和并发更新器进入安装目录，同时保留未更新时的单实例唤醒。
- 安装目录移动、回滚、卸载器保留和 launcher 复制遇到短暂占用时有限重试；长期占用会显示具体阶段和路径。
- 更新失败写入独立轮转日志；半成品 launcher 立即清理，过期 launcher、包、清单和结果缓存会在后续检查时清理。
- 新增真实文件占用、跨线程 Mutex 交接和隔离安装目录更新回归。

重要：`v0.2.7` 及更早版本的旧更新器不包含本次修复。若已经出现文件占用或“下载完成后程序退出且没有后续”，请从本 Release 手动下载 Setup 或 ZIP 覆盖安装 `0.3.0` 一次。用户配置与备份不会被覆盖安装删除。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。
