# PathEcho 0.2.6

PathEcho 0.2.6 补齐未保存设置和自动更新交接的失败边界。

- 从设置页导航到其他页面、托盘退出或自动更新准备退出时，都会复用同一套未保存确认；继续编辑不会退出，放弃更改会恢复已保存配置。
- 致命异常退出不会被未保存提示阻断，避免程序停留在不可恢复状态。
- 更新器启动前仍可取消；外部更新器启动后窗口进入不可取消交接状态，启动握手不再因关闭窗口而遗留等待进程。
- 更新失败时会区分“已安全回滚”和“回滚不完整”；回滚不完整会保留恢复现场并停止自动重启，避免启动未确认的新目录。
- 修复同步任务编辑、复制和删除以及游戏删除失败后，界面仍错误显示成功的问题。

如果 `v0.2.4` 已出现“下载完成后程序退出且没有后续”，请从本 Release 手动下载 Setup 或 ZIP 覆盖升级一次。用户配置与备份不会因覆盖安装而删除。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。
