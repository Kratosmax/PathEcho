# PathEcho 0.2.7

PathEcho 0.2.7 修复自动更新结果文件异常时仍可能没有可见反馈的问题。

- 更新结果 JSON 损坏、缺字段或路径校验失败时，始终写入 Critical 日志并显示通用恢复说明，不再依赖 Debug 日志开关。
- 成功或失败结果显示后再单独清理结果文件；清理失败只记录诊断，不会把已显示的成功误报成读取失败。
- 保留 `0.2.6` 的未保存设置门禁、更新交接不可取消状态、ready 握手与回滚不完整保护。

如果 `v0.2.4` 已出现“下载完成后程序退出且没有后续”，请从本 Release 手动下载 Setup 或 ZIP 覆盖升级一次。用户配置与备份不会因覆盖安装而删除。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。
