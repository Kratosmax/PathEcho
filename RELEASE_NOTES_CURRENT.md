# PathEcho 0.4.0

PathEcho 0.4.0 重构游戏存档快照存储，并修复目录监听、双向删除语义和同步扫描中的可靠性问题。

- 新快照使用“完整逻辑清单 + SHA-256 对象库”，相同内容只保存一份，不再创建会联动修改多个历史版本的硬链接视图。
- “打开备份”会生成独立普通目录副本，用户修改浏览副本不会影响历史版本。
- 文件变化备份只重新读取新增、修改、删除和改名路径；重点文件、定时、游戏退出及监听溢出仍执行整目录快照。
- 旧版 `files` 快照继续支持浏览、迁移和回档；全部对象验真后安全升级，损坏对象会回退仍有效的旧视图。
- 备份迁移与导入按清单复制唯一对象并逐项校验；同一游戏 ID 位于不同根目录的备份不再错误合并。
- 修复同大小、同时间戳的内容变化可能被哈希缓存漏掉的问题，并在读取活动存档时检查文件是否保持稳定。
- 修复双向同步中“忽略删除”仍可能被冲突偏好转换为删除动作的问题。
- watcher overflow 改用独立原子标记，后续普通事件不会覆盖全量恢复要求；单向同步也会监听目标端并自动校准外部修改。
- 普通同步由每轮最多四次全目录枚举降为两次；仅生成冲突副本时额外重扫以建立准确基线。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。

重要：`v0.3.0` 及更早版本若已经出现旧更新器无法移动安装目录的问题，请从本 Release 手动覆盖安装一次。用户配置与备份不会被覆盖安装删除。
