# PathEcho 0.2.1

PathEcho 0.2.1 修复表格双击闪退，并加强同步、备份、配置保存和退出流程的一致性。

- 修复双击同步路径等只读表格单元格时，WPF 尝试回写只读属性导致程序退出的问题。
- 双击同步任务与运行进程时只响应真实数据行，避免按钮、表头和空白区域误触发。
- 手动同步与后台监听共享完整的基线事务锁，避免并发任务用旧基线覆盖新状态。
- 手动备份与后台备份复用同一游戏服务锁，避免同一存档并发生成快照。
- 配置新增、删除和更新先持久化再切换内存状态；单个监听启动失败不再阻止整个应用启动。
- 默认备份目录迁移失败时使用不可取消的回滚，并在迁移期间停止相关监听。
- 退出时等待后台监听清理完成；无法恢复的 UI 异常会写入错误日志并安全退出。
- 运行进程读取失败时恢复列表状态并显示明确错误，不再留下不可操作窗口。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。用户配置和备份数据不会因覆盖安装而删除。
