# PathEcho 0.2.0

PathEcho 0.2.0 改进游戏配置、更新线路、诊断能力和品牌一致性，并修复创建任务时的 UI 线程错误。

- 修复新建同步任务和游戏存档时可能出现的 WPF 跨线程异常。
- 添加运行中进程选择器，可按程序名、PID、窗口标题或路径搜索并回填游戏程序。
- 更新设置升级为多线路列表，支持多个 URL 前缀、0 到 10 优先级、禁用和直连兜底。
- 添加可选 Debug 日志、滚动保留和日志目录入口。
- 修复侧栏悬停对比度，并加入统一 PathEcho Logo、窗口图标、任务栏图标、托盘图标和安装器图标。
- 重构同步任务页：加入弹性列宽、高对比选中态、搜索、状态筛选、目录异常提示、双击编辑和右键快捷操作。
- 隔离预览模式与真实用户配置，并阻止预览执行同步、备份、回档、导入和更新网络操作。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。用户配置和备份数据不会因覆盖安装而删除。
