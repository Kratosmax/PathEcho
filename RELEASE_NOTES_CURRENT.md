# PathEcho 0.2.5

PathEcho 0.2.5 修复未保存内容丢失和自动更新退出后无反馈的问题。

- 同步任务、游戏存档编辑窗口在内容变化后关闭、取消或按 Esc 时，会先确认是否放弃未保存更改。
- 离开有未保存内容的设置页时会提醒；确认放弃后恢复已保存设置，继续编辑则留在当前页。
- 主程序启动外部更新器后先执行存活握手，更新器若因缺少依赖或参数错误立即退出，主程序不再跟着退出。
- 更新器持久化成功或失败结果；父进程退出后更新失败会回滚并重启旧版显示具体原因。
- 新版必须完成初始化并报告就绪后，更新器才删除旧版备份；新版提前退出或超时会自动回滚。
- launcher 会同时复制更新器及其 PathEcho.Core 依赖，兼容单文件和多文件发布布局。

如果 `v0.2.4` 已出现“下载完成后程序退出且没有后续”，请从本 Release 手动下载 Setup 或 ZIP 覆盖升级一次。之后的自动更新会提供失败反馈和回滚保护。

Full 包自带 .NET 8 运行时；Lite 包需要预先安装 .NET 8 Desktop Runtime x64。
